export const EMBED_TWEET_POWERUP = 'embed_tweet_powerup';

export const SETTING_IDs = {
  compactCard: 'embed_tweet_compact_card',
  align: 'embed_tweet_align',
  theme: 'embed_tweet_theme',
};

export type THEME = 'dark' | 'light';
export type THEME_SETTING = THEME | 'auto';
